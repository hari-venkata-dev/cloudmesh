services:

  mosquitto:
    image: eclipse-mosquitto:2
    container_name: cloudmesh-mosquitto

    ports:
      - "1883:1883"

    volumes:
      - ./infrastructure/docker/mosquitto.conf:/mosquitto/config/mosquitto.conf

    restart: unless-stopped